%h1
  ="Page title"
= form_for @image, :url => images_path, :html => { :multipart => true } do |f|
  = f.label :title
  = f.text_field :title
  = f.file_field :picture
  = f.submit
- @images.each do |img|
  = link_to(image_tag(img.picture.url(:thumb)), image_path(img.id))
